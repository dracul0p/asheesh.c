#include<stdio.h>
int main(){
    // int is_sunday=1;
    // int is_snowing=1;
    // printf("%d \n " ,is_sunday  && is_snowing);

    // int is_monday=0;
    // int is_raining=1;
    // printf( "%d \n",is_monday || is_raining);

    int n;
    printf("enetr a no:");
    scanf("%d",&n);
    printf("no is greater than 9 and less tha 100 if :%d ",n>9 && n<100);
    return 0;

}