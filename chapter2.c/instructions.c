0#include<stdio.h>
#include<math.h>
int main(){
    //  instructions
    // declare variable before using

// type declaration instructions
    int a=22;
    int b=a;
    int c=b+1;
    int d=1,e;
    int a,b,c;
    a=b=c=1;
    // use and declare should be done together  int x =y= z =4;

    int oldAge= 22;
    // error beacuse year is used before declaration
    // int youngAge= oldAge + years;

    int years= 2;

    // aritmetic instructions a+b
    int a=1, b=2;
    int sum=a+b;
    // a , b is  operant , + is operator
    // single variable on the LHS
    int x, y = a*b;  
    // here y will have value of a*b , x is just a variable
    int multiply = a * b;

    // valid a=b+c   a=b*c   a= b/c
    // invalid b+c=a a=bc a=b^c
    // operators  + -  /  %
    // pow(x,y) for x to power y
    int x = 3;
    int y = 2;
    int power = pow(x,y);
    printf("%d" ,  power);
    // b ^ c  exor  operator bit wise operator 

// control instructions
// control instructions
}