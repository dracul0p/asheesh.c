// function:block of code that perform aperticular task.
// fn takes argument--->do work --->return result
// can be used multiple times increase code reausibility 

/*  syntax 1
     function prototype             //tells compile that  we are declaring function
     
     void printHello();            // ()PARANTHESIS,parameter options    ; STATMENT TERNINATOR     VOID: EMPTY FUNCTION   WILL WORK BUT NOT RETURN ANY VALUE


    syntax2 
    function defination

    void printHello(){                    
        printf("hello");      // funcation defination
    }     
    
    syntax3          
    function  call
     int main(){
        printfHello();           //calling function: after main or any other function
        return 0;
    }                              */


/*   1. function always start from main
   2.function gets call  directly and indirectly from main \
   3. there can be multiple function in a program                   */
    
/*  types of function:
    1.library function: in built in c e.g scanf(), printf()
    2. user defined: declare & define by programmer                 */

// passing arguments:
//  function can take some value i.e PARAMETER &   give some value i.e    RETURN value

/*PASSING ARGUMENTS

  void printhello();           // no return
  void printtable(int n);     //  fn no return value but   takes value as parameter
  int sum(int a,int b);       // return value but also  takes 2 value in parameter    */